#include<stdio.h>
int main()
{
    int n,count=0;
    printf("enter a number =");
    scanf("%d",&n);
    for(int i=2;i<n;i++)
    {
        if(n%i==0)
        {
            count++;
            break;
        }
        else if(count = =o)
        {
            printf("not prime number");

        }
        else
        {
                printf("prime number");
    }
}
